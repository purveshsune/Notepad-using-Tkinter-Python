# Notepad-using-Tkinter-Python

Added a feature to the already existing code
Feature -  While closing the notepad it asks "Are you sure you want to close?" if not, then the window stays, if yes, the notepad closes
